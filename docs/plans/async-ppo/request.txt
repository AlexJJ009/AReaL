这里补几个说明：

1. 如果我们用 PyTorch 的 FSDP2 后端是可以的，因为我们之前在 vLLM 0.9.0 里面好像用的其实也是 FSDP2。Megatron 应该在那种多机场景下用会好一点，否则太重了，反而不适合。
2. 新安装默认选择 SGLang，我觉得是可以的。
3. 我们也确实要用 uv 来管项目依赖和 lock 文件。
4. 当前没有一个 AReaL 的 release 可以直接跑 SLO/SAO 目标，所以这玩意儿我们肯定之后会自己造。

接下来我授权你去和 vLLM 0.9.0 平级的目录下，新建一个专门放 AReaL 的代码仓库，建一个 AReaL 的分支，并在 worktree 里面去实现 SAO



然后实现 SLO 之前，我们要先验证这一套依赖可以跑通。



验证的办法就是：

我们拿 Qwen 3.5 4B base model，用单机（这台机器所有的八卡）跑一个异步的 GRPO。我们用 GRPO 的 loss 来跑，就是跑绝对 standard 的 GRPO，要区别于 DAPO。







关于 Docker 盘的容量：由于之前我们想来跑一些 Agentic 的训练，或者说一些 OPSD 的训练任务，所以 Docker 盘估计被占了很多，现在是可以清理一下的，我授权你去清理。



另外，官方 SGLang 的镜像压缩层，现在用的是这种固定镜像，这确实挺好的，但可能会占用我们较多的镜像空间。



所以其实我很想知道，我们能不能直接在 uv 里面照着 Dockerfile 重新安装一遍依赖？这样可以节省出一些 Docker 盘的空间。因为 Docker 盘空间就那么点，被占用之后，我们但凡跑任何 Agentic 任务就没得用了





跑 GRPO 的参数，我觉得基本用默认那套就可以了。不过可以根据我们 A800 机器的 batch size 做一些适配微调：把 response length 设置为 8K，训练数据集用 DAPO。



测试集用我们之前用过的那 5 个测试集。其中我要求你对 AIME 2024、AIME 2025 和 Beyond AIME 这几个数据集本身做一次筛查。



之前出现过像“043”和“43”被区分开的情况。虽然我们之前单独写过打分器做自动识别，但这不能从根本上解决问题。根本的解决办法是照着现有的测试集改一下，把这类坏 case 改对：原本答案 ground truth 是 043 的，直接改成 43。



所以这块我其实会有一个疑惑：我们应该跑异步的 GRPO 好呢，还是跑异步的 PPO 比较好？我们也不用立马先把 SAO 跑通，其实跑一下 PPO 就可以了。



用 4B dense 的 model 来跑 PPO 是不是也可以？官方有 PPO 的配置，我们其实可以照用一下，把它改成用在 DAPO 数据上去跑：

• 回答长度（response length）可以开到 8K

• batch size 都可以调高一点（你可以参考我们之前跑 PRD 算法用的相关配置）



由于这种 actor 和 critic 的设置，我觉得还是可以做四卡训练、四卡 rollout。因为这是异步的，我们一定要先把异步的跑通。



你觉得当前这样一套方案是否可行？我觉得我们先把异步的 PPO 跑通，这个是否可行？

如果可行的话，我们可以先出一份简单的 PRD，要求把依赖安装完、把实验跑完。跑通实验的验收标准，就是要监督 5 个 step，只有这 5 个 step 通过了，任务才算完成。

我们需要去验证的事项：

1. 依赖安装完成
2. 本机已有的模型权重可以正常加载
3. Docker 盘已被清理
4. 数据集已清扫完毕（之前的数据集和测试集格式都已校对）

具体的 case study 在之前的对话中已经有了，你可以查阅之前的记录。这件事情可以 spawn 一批 SubAgent 来协助你做。

所以我觉得现在应该先产出一份 PRD，把协议和我们要验收的 checklist 定出来。



等到我们先把 PPO 跑通了，出了第一版结果之后，我们再去跑 SAO。



SAO 也是在这种 dense 的场景、纯数学的场景去跑。之前我们用 MATH 数据集跑，效果太烂了，我觉得 MATH 数据集根本学不会 AIME，只有 DAPO 数据集才可能学会。

--- 后续用户补充（原文） ---

我们的 checklist 或者协议里面，还要考虑一些：

首先是打分器格式对不对？打分器可以参考我们在实现 PRD 算法时用的那个打分器，它应该包含基本的正则，以及 LaTeX verify 这种语义匹配。

然后在启动之前，应该去核查以下几点：

1. loss 对不对？
2. 两个模型的加载对不对？
3. 推理加速有没有开？
4. Actor update 加速有没有开？
5. 由于用的是 FSDP2，所以 Flash Linear Attention 以及像是 Convolution 1D 那种加速的算子有没有开？我想在 SGLang 里面应该也有对应的优化，在 FSDP2 里面的优化应该是共用的。也就是说，要验证 Actor Update 也有被加速，因为 3.5 4B Base 它有 24 层的 Linear Full Attention，只有 8 层的 Full Attention
6. 然后是 Areal 的 validation 和 save frequency 的频次，我觉得可以设置为每 20 个 step 存一下，然后对着 5 个 test set 测一遍。



   由于这是跑一个 demo，只需要测 N 等于 4 就可以，不需要测 N 等于 16
7. 我们使用的 DAPO 数据集不应该用我们那个子集了，而是直接固定一个种子之后跑全量的就可以了。



   这是因为当前的 step 数量估计可以很多，所以跑全量的整个数据集完全没问题。
8.

--- 总训练长度澄清（用户回答原文） ---

跑完整 1 epoch；前 5 step 逐步监督，之后每 20 step 保存并评测 N=4

--- 执行与报告授权（用户原文） ---

用户要求：PRD制出来后，checklist定好后，就执行，通过所有checklist验收才算任务完成。
[$work-report](/data_storage/yl_test/lgx/home/.agents/skills/work-report/SKILL.md) 关键决策记录，完成后交一份结果和决策报告给用户看。
